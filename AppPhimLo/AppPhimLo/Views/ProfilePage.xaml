<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppPhimLo.Views.ProfilePage">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20" BackgroundColor="#191b24">

            <!-- Avatar -->
            <Frame CornerRadius="80"
                   HeightRequest="160"
                   WidthRequest="160"
                   BackgroundColor="White"
                   HorizontalOptions="Center"
                   HasShadow="True"
                   Padding="0">
                <Image Source="anh1.png"
                       Aspect="AspectFill"
                       HeightRequest="100"
                       WidthRequest="100"/>
            </Frame>

            <!-- Name -->
            <Label x:Name="NameLabel"
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="#212121"/>

            <!-- Divider -->
            <BoxView HeightRequest="1"
                     BackgroundColor="#E0E0E0"
                     Margin="0,20"/>

            <!-- Card thông tin bổ sung -->
            <Frame CornerRadius="12"
                   BackgroundColor="White"
                   HasShadow="True"
                   Padding="15">
                <VerticalStackLayout Spacing="10">

                    <Label Text="Thông tin liên hệ"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#212121"/>

                    <!-- Email -->
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="📧"
                               FontSize="16"
                               VerticalOptions="Center"/>
                        <Label x:Name="EmailLabel"
                               FontSize="16"
                               TextColor="#424242"/>
                    </HorizontalStackLayout>

                    <!-- Số điện thoại -->
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="📞"
                               FontSize="16"
                               VerticalOptions="Center"/>
                        <Label x:Name="PhoneLabel"
                               FontSize="16"
                               TextColor="#424242"/>
                    </HorizontalStackLayout>

                    <!-- Địa chỉ -->
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="🏠"
                               FontSize="16"
                               VerticalOptions="Center"/>
                        <Label x:Name="AddressLabel"
                               FontSize="16"
                               TextColor="#424242"/>
                    </HorizontalStackLayout>

                    <VerticalStackLayout Padding="20">
                        <Button Text="Xem Phim"
                        Clicked="OnProfileButtonClicked" />
                    </VerticalStackLayout>


                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
